# Address a Linear issue

Follow `.claude/skills/address-linear-issue/SKILL.md`.

Read the Linear issue, implement the fix, then hand the PR to a **fresh**
Cursor Grok (standard — never the fast variant) subagent running
`code-review-skill`. This session then dispositions the review with
`/address-review`. Do not review your own diff.
