# Grill with handbook

Follow `.claude/skills/grill-with-handbook/SKILL.md`.

Sharpen the plan with the maintainer; record decisions as light notes on
`dev-docs/formats/*` or `dev-docs/topics/*` (not a new ADR by default). Do not
implement until they confirm shared understanding and ask to build.

Loop context: `dev-docs/pair-workflow.md`.
