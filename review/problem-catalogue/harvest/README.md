# Harvest — per-capability problem drops

Raw output from [Topic 8](../../docs-content/brief.md)'s capability workers, one file per
capability, before merging into `../catalogue.md`.

These are **drops, not entries**: unnormalized, possibly duplicated against each other and
against the registers, and not yet held to the neutrality rule. Merging and rewriting them
is step 3–5 of [`../brief.md`](../brief.md) §Suggested process.

Why they exist at all: most of this residue never reached a register. A grep for
`workaround|quirk` over `src/` finds four sites, while 136 comments name an upstream
library — it is not keyword-findable, so it is only reachable by someone already reading
the subsystem. Topic 8's workers are the only readers who will be.
