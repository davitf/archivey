# Worker I — Coordinator-owned (docs shape, positioning, attribution)

You are a **verification worker** for Topic 8 pass 1 for cluster I
(documentation shape / attribution / positioning claims). Verify only.
Do not edit `docs/` or `src/`. Do not write how-it-works.md.

## Lessons
These rows are checkable facts (license paths, `__all__` counts, env-var
names, dangling IDEAS.md refs), not persuasion (Topic 7).
Prefer `openspec/specs/documentation` when cited (O-26).
Defect/silence/count claims that are true → verified.
Harvest capped (~6–10).

## Inputs
- SESSION.md, cluster-I.md (I-1…I-24)
- Spec: `documentation`; also check `archivey.__all__`, licence files,
  acknowledgements paths

## Outputs
1. `/workspace/review/docs-content/worker-inputs/verdicts-I.md`
2. `/workspace/review/problem-catalogue/harvest/I-docs-shape.md`

Use `uv run --no-sync`. Summary: counts, wrong IDs.
