# Tasks — TAR names decode as UTF-8 by default

- [x] 1.1 Pass `encoding="utf-8"` to `tarfile.open` when the caller passes none.
- [x] 1.2 Tests under a simulated non-UTF-8 locale; docs, handbook and changelog.
- [x] 1.3 Archive this change.
