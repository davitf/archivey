# Tasks — CLI member pattern directories

- [x] 1.1 Match patterns with and without a trailing `/`, plus the directory's contents.
- [x] 1.2 Read `\` as `/` on Windows only.
- [x] 1.3 Use the same matching for `--exclude` and the unmatched-pattern warning.
- [x] 1.4 Tests, `docs/cli.md`, archive this change.
