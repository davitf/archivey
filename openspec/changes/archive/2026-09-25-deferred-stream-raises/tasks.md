# Tasks — deferred stream raises

- [x] 1.1 Spec: the diagnostics policy requirement and the seek-index matrix row.
- [x] 1.2 Docstrings: `deferring_raises` and `escalate_only`.
- [x] 1.3 Test: two thinnings inside one `read(-1)` raise once; `read(700)` raises once per call.
- [x] 1.4 `openspec archive deferred-stream-raises --yes`
