# Tasks — PPMd child death verdicts

## 1. Spec

- [x] 1.1 Name every child-death outcome in the `max_ppmd_in_process_input` sentence, and
      add a scenario row.
- [x] 1.2 Archive this change into `openspec/specs/`.
